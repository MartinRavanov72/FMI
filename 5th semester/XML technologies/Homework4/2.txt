//cd[@id="8c0a600b"]//track[@length="4:04"]         // търсим в цялата йерархия: избираме елемента cd с атрибут id с конкретната стойност и после елемента track с атрибут length = "4:04"

//track[position() mod 2 = 0]           // избираме елемента track, който има четна позиция
//track[position() mod 2 = 1]           // избираме елемента track, който има нечетна позиция

//track[string-length(text()) > 35]         // избираме елемента track, чиято дължина на стойността е по-голяма от 35

//track[string-length(text()) > 15]/string-length(text())           // избираме елемента track, чиято дължина на стойността е по-голяма от 15 и после взимаме дължината им

//cd//track[last()]         // търсим елемента track, който се намира някъде в йерархията на елемента cd, и взимаме последния такъв

//cd//track[5]          // търсим елемента track, който се намира някъде в йерархията на елемента cd, и взимаме петия такъв

//cd/count(./*/track)         // избираме елемента cd, после намираме броя на елементите track в неговата йерархия

//track[matches(text(), "Ya soshla s uma")]         // търсим елемента track, който съдържа конкретния стринг в себе си

//track[starts-with(text(), "D")]           // търсим елемента track, който започва с "D"

//track[ends-with(text(), "sta")]           // търсим елемента track, който завършва с "sta"

//track/upper-case(text())          //избираме елемента track и връщаме стойностите му, но с главни букви

//cd[./*/track[matches(text(), "^Robot \(Robotronik\)$") and @length="3:55"]]/year          //търсим елемента year, който е под-елемент на cd, което съдържа елемент track с конкретната стойност и атрибут length = "3:55"

sum(//cd/count(./*/track)) div count(//cd)          // взимаме сумата на броя на всички елементи track в cd и я делим на броя на всички елементи cd

//cd/[concat("title_", title), concat("year_", year)]           // взимаме елементите cd с конкатенирани стойности "title_" и елемента title, както и "year_" и елемента year